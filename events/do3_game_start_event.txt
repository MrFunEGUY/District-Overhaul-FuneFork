namespace = do3gamestart

event = {
	id = do3gamestart.01
	hide_window = yes
	is_triggered_only = yes
	fire_only_once = yes
	trigger = { is_do3_active = yes }
	immediate = {
		set_global_flag = do3_active
	}
}